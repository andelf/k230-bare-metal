# default target

[build]
target = "riscv64gc-unknown-none-elf"


[target.riscv64gc-unknown-none-elf]
rustflags = [
    "-C",
    "target-feature=+zve64d,+zvl128b",

    #"-C",
    #"target-feature=+zaamo,+xtheadbb",
    #"-C",
    #"link-arg=-Tlink.x",
    #"-C",
    #"link-arg=--Bstatic",
    #"-C",
    #"link-arg=--relocatable",
]


[unstable]
build-std = ["core"]

[env]
DEFMT_LOG = "info"
